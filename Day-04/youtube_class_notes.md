*function creation*
```
def function_name():
  linesof code
  retun
function_name()
```
*modules*
instead of writting same python code multiple code use same file in another file also
```
import file1 ###importing the file which is called module
file1   ###running full module
file1.addition()  ###running only addition function
```
*packages*

packages are **collection of modules**

pypi  -- *python package index*
  open source plattform where python packages/modules are present 
  these modules can be installed by uing **pip**
   ```
   pip install <package_name>
   pip list ###to check the installed packages
   ```

*Virtual env*
 logical seperation of vms to use different 
  
